name: Build Windows (exe + MSI)

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      # Build x64 uniquement pour éviter le conflit WiX (fichier verrouillé) sur le runner
      - name: Build Windows (NSIS + MSI, x64)
        run: npx electron-builder --win --x64

      - name: Upload Windows installers
        uses: actions/upload-artifact@v4
        with:
          name: windows-installers
          path: dist/
